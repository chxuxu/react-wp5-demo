import React,{useState} from 'react';
interface ILoginInfoProps {
  username?:string
}

export default function LoginInfo (props: ILoginInfoProps) {
  return (
    <div className="m-LoginInfo">
      LoginInfo
    </div>
  );
}
