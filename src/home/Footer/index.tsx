import React, { useState } from 'react';
import { Nav } from "rexui";
interface IFootInfoProps {
  icon?: string
}

export default function FootInfo(props: IFootInfoProps) {
  return (
    <div className="m-FootInfo">
      FootInfo
    </div>
  );
}
